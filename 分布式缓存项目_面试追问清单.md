# 基于 gRPC + etcd 的分布式缓存系统：校招面试追问清单

项目关键词：C++、gRPC、HTTP/2、etcd、服务发现、一致性哈希、虚拟节点、LRU、TTL、SingleFlight、防击穿、三态熔断、fallback、过期缓存降级、Prometheus、ghz 压测、perf + 火焰图。

使用方式：这个项目的面试重点通常会集中在“为什么要分布式缓存、如何路由、如何防击穿、如何处理节点故障、压测指标是否可信”。准备时不要只背概念，要能讲清每个异常场景。

## 1. 项目整体介绍

### 1.1 三分钟项目介绍

**面试官：介绍一下你的分布式缓存项目。**

**回答：**这是一个使用 C++17 实现的类 GroupCache / 类 Memcached 分布式缓存原型。客户端从 etcd 发现缓存节点，在本地通过一致性哈希和虚拟节点为 key 选择负责节点，再复用 gRPC Channel 发起请求。缓存节点内部用按字节容量限制的 LRU 和 expire-after-write TTL 保存数据；本地未命中时，通过 SingleFlight 合并同 key 回源，并用三态熔断、stale cache 和 fallback 控制后端故障的影响。写路径使用目标节点写入加其他节点失效广播，因此系统提供最终一致性，而不是强一致性。

这个项目的重点不是替代 Redis，而是完整走通服务发现、客户端路由、本地缓存、防击穿、熔断降级和性能分析这些机制。当前没有持久化、副本故障转移、可靠失效消息和生产级数据迁移，所以更准确的定位是工程学习与验证项目。

连环追问与回答：

- **这个项目解决什么问题？**
  它解决多节点缓存中的节点发现、key 路由、容量扩展、热点 key 失效后的回源击穿，以及缓存节点或后端数据源故障时的快速失败和降级问题。
- **为什么需要分布式缓存，而不是单机缓存？**
  单机缓存受单进程内存、CPU 和故障域限制。分布式缓存可以横向扩容并分散流量，但代价是必须处理路由、节点变化、缓存冷启动和数据一致性；本项目正是在实现这些新增问题的解决机制。
- **为什么说是类 GroupCache？**
  因为借鉴了 Group 命名空间、按 key 选择 peer、getter 回源和 SingleFlight 合并请求的思想。但项目没有直接移植 Go groupcache，也没有完整实现它的 peer fetch 和副本语义。
- **客户端、缓存节点、etcd、后端数据源分别做什么？**
  客户端负责服务发现、本地路由、Channel 复用和节点级熔断；缓存节点负责 LRU/TTL、SingleFlight、回源熔断和降级；etcd 只承担注册发现的控制面；后端数据源由 DataGetter 抽象，是缓存 miss 后的真实数据来源。
- **请求一个 key 的完整流程是什么？**
  客户端从本地哈希环选节点并发起 gRPC Get；服务端先查 fresh LRU，命中直接返回；未命中则经过 breaker 和 SingleFlight，由 先锋线程 回源，成功后写 fresh/stale cache 并广播结果给 waiter，失败则走 stale/fallback。
- **你实现了哪些核心模块？**
  包括 etcd registry/discovery、一致性哈希与虚拟节点、gRPC Client/Server、Channel 缓存、LRU/TTL、SingleFlight、CircuitBreaker、失效广播、Prometheus 指标和 ghz/perf 压测脚本。
- **项目里最难的点是什么？**
  难点不是单个数据结构，而是异常语义的组合：pioneer 和 waiter 的不同超时、一次真实失败只上报一次 breaker、旧请求结果不能污染新一代熔断状态，以及 detach 回源无法取消所带来的资源风险。

建议回答结构：
- 背景：多个缓存节点共同承载热点数据，客户端通过服务发现和一致性哈希定位节点。
- 核心能力：服务发现、一致性哈希、LRU/TTL、SingleFlight、防击穿、熔断降级、Prometheus 指标。
- 性能：ghz 压测全链路 QPS 和 P99；裸测用于验证本地缓存结构上限。
- 异常：节点上下线、回源超时、缓存过期、连接复用和熔断降级。

### 1.2 项目真实性追问

**面试官：这个项目参考了 GroupCache，哪些是参考思想，哪些是你自己实现？**

**回答：**参考的是 GroupCache 的设计思想，不是代码移植。项目将这些思想换成 C++17、gRPC 和 etcd 的实现，并扩展了 TTL、三态熔断、stale/fallback、失效广播、Prometheus 和性能分析。面试时应明确区分“参考架构思想”和“自己实现模块”，避免把借鉴说成原创算法，也避免把自行编码说成直接调用现成框架。

连环追问与回答：

- **是否直接使用了 groupcache 代码？**
  没有。Go groupcache 不能直接用于 C++ 项目，这里参考其 Group、peer picking 和 SingleFlight 思想后重新实现。
- **gRPC 接口是自己定义的吗？**
  是。项目在 proto 文件中定义 Get、Set、Delete 和 Invalidate 等 RPC，再由 protoc 生成 C++ Stub 和 Service 基类。
- **etcd 服务发现是自己封装的吗？**
  是。底层使用 etcd-cpp-apiv3，项目自行封装注册 key、10 秒 lease、keepalive、首次 list 和 watch 回调，以及节点表和哈希环更新。
- **LRU/TTL 是自己写的吗？**
  是。使用 std::list + std::unordered_map，容量按 key/value 字节统计，entry 保存绝对过期时间并在 Get 时惰性删除。
- **SingleFlight 是自己实现还是移植？**
  是按思想自行实现。当前版本使用 promise/shared_future、Call map、waiter timeout、失败冷却期和一次失败上报控制，已经超出最简版 SingleFlight。
- **Prometheus 指标暴露用了什么库？**
  项目没有引入完整 Prometheus C++ Client，而是用 cpp-httplib 暴露 /metrics，手工生成 Prometheus text exposition format。
- **压测是在单机还是多机？VM 8C8G 是什么环境？**
  是单 VMware 虚拟机、8 vCPU，客户端和服务端通过 loopback 通信。它能用于相同环境下的方案对比和瓶颈归因，不能代表真实多机网络或生产容量。

注意：
- 可以说参考了 GroupCache 的 peer picking、singleflight 思想。
- 重点放在你如何用 C++、gRPC、etcd 实现这些机制。

## 2. 整体架构与请求链路

### 2.1 架构图讲解

**面试官：画一下系统架构。**

**回答：**架构可以分成控制面和数据面。控制面由缓存节点向 etcd 注册地址并续租，Client SDK 首次全量拉取节点并持续 watch。数据面不经过 etcd：Client SDK 在本地哈希环选节点，复用该节点 Channel 发起 gRPC；服务端依次经过 Group、fresh LRU、SingleFlight、breaker 和 getter，失败时转 stale/fallback。每个缓存节点可独立暴露 /metrics。

调用关系是：Caller -> KCacheClient SDK -> ConsistentHash/Channel cache -> gRPC -> KCacheServer -> Group -> LRU/SingleFlight/getter；etcd 位于 SDK 和节点旁边承担注册发现，Prometheus 从节点拉取指标。

连环追问与回答：

- **客户端如何发现缓存节点？**
  启动时读取 /services/<service_name>/ 前缀，之后 watch 该前缀的 PUT/DELETE 事件。
- **etcd 存了哪些信息？**
  当前 key 是 /services/<service>/<addr>，value 也是节点地址，并绑定 10 秒 lease。生产中可扩展权重、机房、版本和协议能力。
- **客户端如何维护本地节点列表？**
  cache_nodes_ 保存地址集合，nodes_mutex_ 保护节点集和路由更新；watch 回调负责 Add/Remove。
- **一致性哈希环在哪里维护？**
  每个客户端各自维护一份 ConsistentHashMap，etcd 不负责计算路由。这减少了中心路由 hop，但会产生客户端视图短暂不一致。
- **请求 key 如何映射到节点？**
  对 key 做 CRC32，在排序虚拟节点数组中 lower_bound，选择顺时针第一个虚拟节点对应的真实地址。
- **本节点命中、远程节点命中、回源分别是什么流程？**
  当前 SDK 没有“目标就是本机则进程内直调”的分支，统一通过 gRPC 到目标节点。目标节点 fresh LRU 命中就返回；未命中才在目标节点执行 SingleFlight 和本地 getter 回源。
- **Prometheus 指标从哪里暴露？**
  KCacheServer 在配置了 metrics port 后启动独立 HTTP 端口，通过 /metrics 输出每个 Group 的计数和状态。

应覆盖模块：
- Client SDK。
- etcd registry / discovery。
- Consistent hash ring。
- gRPC channel pool。
- Cache server。
- Local LRU/TTL cache。
- SingleFlight manager。
- Backend loader。
- Circuit breaker。
- Metrics exporter。

### 2.2 请求链路

**面试官：以读取一个 key 为例，讲完整流程。**

**回答：**客户端调用 Get(group, key) 后，先从本地一致性哈希环选出 target_addr，再从 Channel map 获取或创建 Channel，构造 Stub 和带 deadline 的 ClientContext 发起同步 Unary RPC。服务端先找到 Group，再执行 KCacheGroup::Get。fresh LRU 命中直接返回；miss 才进入 Load，先向服务端 breaker 申请 permit，再进入 SingleFlight。pioneer 执行 LoadData，成功写入 fresh/stale cache 并唤醒 waiter；失败则按 stale cache、fallback getter、空结果的顺序降级。Protobuf response 最后经 gRPC 返回 SDK。

连环追问与回答：

- **客户端拿到 key 后如何选择节点？**
  在 nodes_mutex_ 保护下调用本地 consistent_hash_.Get(key)；空节点集直接失败，哈希异常时当前代码退化为节点集合中的第一个地址。
- **如果目标节点就是本机，怎么处理？**
  当前没有本地短路优化，仍走 gRPC。这样实现一致，但会保留 loopback 协议开销；生产 SDK 可识别本地节点并直调，但要统一鉴权、指标和错误语义。
- **如果是远程节点，如何通过 gRPC 调用？**
  从按地址复用的 Channel 创建 Stub，设置 RPC deadline，然后调用同步 Get。
- **服务端收到请求后先查缓存还是先做熔断判断？**
  先查 fresh cache，只有 miss 才检查回源 breaker。否则后端熔断会误伤完全可以本地命中的请求。
- **缓存命中直接返回什么？**
  返回 ByteView 对应的数据，并增加 local_hits；服务端将其写入 Protobuf response。
- **缓存未命中如何回源？**
  增加 local_misses 后进入 Load，由 SingleFlight 保证同 key 通常只有一个 pioneer 调 getter。
- **回源结果如何写入缓存？**
  getter 成功后写 fresh cache，使用 cache_ttl_ms_；同时写 stale cache，使用独立 stale_ttl_ms_，然后将结果发布给 waiter。
- **回源失败时是否返回 fallback 或过期缓存？**
  是。优先查 stale cache，再调用 fallback getter，均没有则返回空。该语义只适用于容忍陈旧值的业务。
- **哪些环节会记录指标？**
  当前记录 local hit/miss、loader hit/error、Load 平均耗时、breaker 次数、fallback hit、getter timeout、breaker state 和缓存容量。尚未完整暴露 RPC latency histogram 和 SingleFlight waiter 数。

校招考点：
- 能否把“客户端路由”和“服务端缓存查询”分清楚。
- 能否讲清命中、未命中、故障三条路径。

## 3. gRPC 与 HTTP/2

### 3.1 gRPC 基础

**面试官：为什么用 gRPC，而不是 HTTP REST？**

**回答：**缓存节点间是内部、高频、契约稳定的小消息调用。gRPC 提供 proto 驱动的强类型接口、Protobuf 二进制编码和 HTTP/2 多路复用，减少手写协议和 JSON 解析成本，因此比 REST 更适合当前节点通信。代价是调试不如 JSON 直观、浏览器支持较弱，并且 C++ 同步 gRPC 的线程调度开销在本项目全链路压测中已经成为主要成本。

连环追问与回答：

- **gRPC 基于什么协议？**
  当前 gRPC 传输基于 HTTP/2，消息通常用 Protobuf 编码。HTTP/2 管传输和 stream，Protobuf 管消息序列化。
- **Protobuf 相比 JSON 有什么优势？**
  字段号和 varint 等编码通常使消息更小，生成代码避免大量运行时字段名解析，并提供强 schema。代价是可读性较差且要管理 schema 演进。
- **gRPC 有哪四种调用模式？**
  Unary、Server Streaming、Client Streaming 和 Bidirectional Streaming。
- **你的项目用了哪种调用模式？**
  Get/Set/Delete/Invalidate 都是同步 Unary RPC；没有使用流式或 CompletionQueue 异步服务。
- **deadline/timeout 如何设置？**
  SDK 为每次 ClientContext 设置绝对 deadline。deadline 到期后 RPC 返回 DEADLINE_EXCEEDED，但业务应注意取消信号是否真正传到底层工作。
- **gRPC 状态码和业务错误如何区分？**
  UNAVAILABLE、DEADLINE_EXCEEDED 等表示传输/服务调用结果；key 不存在、fallback 等是业务语义，应由 response 字段或明确业务状态表达。当前接口的表达能力较简单，是可改进点。
- **gRPC 适合内部服务调用的原因是什么？**
  强契约、生成代码、跨语言、HTTP/2 连接复用和标准 deadline/status。它不是天然比 REST 在所有场景更快，仍需结合实现模型和压测判断。

八股延伸：
- HTTP/2 多路复用。
- header 压缩。
- stream。
- TCP 层队头阻塞。

### 3.2 Channel 复用

**面试官：为什么要复用 gRPC channel？**

**回答：**Channel 是客户端到目标服务的长期连接管理抽象，内部可管理地址解析、subchannel、HTTP/2 transport、连接状态和重连。复用 Channel 的目的不是只复用一个 C++ 对象，而是复用底层连接与传输状态，避免每次 RPC 重建连接。当前项目的所谓连接池实际是 addr 到 shared_ptr<Channel> 的缓存，每个节点默认一个 Channel，并不是真正的多连接池。

连环追问与回答：

- **channel 创建成本在哪里？**
  包括目标解析、TCP 建连、HTTP/2 初始化，启用 TLS 时还有握手；还会创建和维护 gRPC 内部状态。
- **channel 和 stub 的关系是什么？**
  Stub 是 proto 生成的类型化 RPC 代理，它借助 Channel 发送调用；Channel 管传输，Stub 管方法接口。当前缓存 Channel，每次 RPC 创建局部 Stub。
- **HTTP/2 连接和 stream 有什么关系？**
  一条 HTTP/2 连接可同时承载多个逻辑 stream，一个 Unary RPC 通常对应一个 stream。
- **频繁创建 channel 会导致什么问题？**
  会失去多路复用，增加握手、端口、内核 socket 和 gRPC 状态开销，并造成延迟抖动。
- **channel pool 如何设计？**
  当前按节点地址做并发安全 map，watch DELETE 时删除。生产上还要加入 idle 回收、容量上限、健康状态和每地址多 Channel 的负载策略。
- **一个节点维护几个 channel 合理？**
  通常先从一个开始，因为一个 Channel 已支持并发。只有实测单连接 stream、流控、TCP 或 gRPC transport 争用成为瓶颈，才增加 Channel 数。
- **channel 失效后如何重连？**
  gRPC Channel 自身会管理连接状态并尝试重连；业务侧用 deadline 和 breaker 避免等待过久。节点从服务发现中下线时再移除 map 引用。

项目追问：
- **你复用的是 channel 还是 stub？** 当前复用 Channel，Stub 是每次请求的局部对象。
- **连接池按节点维度维护吗？** 是，以节点 addr 为 key。
- **节点下线后，channel 如何清理？** watch DELETE 触发 RemoveChannel。已发起 RPC 已复制 shared_ptr，不会被 erase 粗暴销毁，而是在完成或 deadline 失败后释放。

### 3.3 HTTP/2 并发流控制

**面试官：HTTP/2 并发流控制如何影响 QPS？**

**回答：**HTTP/2 允许单连接多 stream，但并发不是无限的。MAX_CONCURRENT_STREAMS 限制连接同时活跃的 stream 数，流控窗口限制可发送但尚未确认的数据量，服务端线程和 ResourceQuota 决定真正可执行的请求量。任何一层先饱和，后续请求都会排队，QPS 到平台后 P99 上升。

连环追问与回答：

- **一个连接上可以并发多少 stream？**
  由双方 HTTP/2 SETTINGS 和 gRPC 配置共同约束，不是协议上固定的一个数字。项目通过 GRPC_ARG_MAX_CONCURRENT_STREAMS 配置上限。
- **max concurrent streams 过小会怎样？**
  多余 RPC 在客户端或 transport 层排队，连接没有充分利用，吞吐下降并拉高尾延迟。
- **连接数过多会怎样？**
  增加 socket、TCP 拥塞状态、内存和调度开销，也可能绕过单连接背压，让服务端瞬间过载。
- **服务端工作线程限制和 HTTP/2 stream 限制有什么关系？**
  stream 上限决定能进入连接的并发，线程/资源配额决定能同时执行的并发。前者高而后者低，只会把排队从连接侧移动到服务端。
- **P99 延迟变高时，如何判断是不是 stream 排队？**
  固定业务负载，对比不同连接数、stream 上限和服务线程配置下的 QPS/P99，并观察 in-flight、CPU 和 gRPC 调用栈。若增加连接或 stream 后 P99 改善且服务端未饱和，才支持该归因。

## 4. etcd 服务发现

### 4.1 服务注册

**面试官：etcd 在你的项目里做什么？**

**回答：**etcd 是控制面注册中心，不保存缓存数据，也不参与每次请求。节点启动后把地址写到服务名前缀并绑定 lease，客户端通过首次 list 和后续 watch 维护节点视图。设计意图是让数据面在 etcd 短暂不可用时仍能使用旧视图工作，同时用 lease 自动清理崩溃节点。

连环追问与回答：

- **服务节点启动时向 etcd 写什么？**
  当前写 /services/<service>/<addr>，value 为地址，并绑定 lease ID。
- **key/value 格式如何设计？**
  key 用层级前缀便于按服务 watch；value 当前简单存地址。生产 value 可用结构化数据保存权重、zone、版本和能力标签。
- **lease 是什么？**
  是有 TTL 的租约，key 与 lease 绑定。当前 lease 为 10 秒，到期后 etcd 自动删除 key。
- **keepalive 是什么？**
  是节点定期续租，证明自己仍存活。当前循环约每 3 秒检查/续约。
- **节点异常退出后，etcd 如何感知？**
  不是主动探测进程，而是收不到续租，lease 到期后删除注册 key，客户端再通过 watch 感知 DELETE。
- **如果 keepalive 失败，节点是否应该停止服务？**
  当前只记录错误并继续循环，没有自我隔离。生产上应在连续续租失败后标记 not-ready 或停止接新流量，否则节点可能仍服务但已从部分客户端视图消失。

八股延伸：
- **etcd 为什么适合注册中心？** 它提供强一致 KV、lease、watch 和 revision，天然支持服务生命周期和配置分发。
- **Raft 大概解决什么问题？** 它让多个 etcd 成员对日志顺序和 leader 达成共识，在少数节点故障时仍保持一致状态。
- **CAP 角度怎么看 etcd？** 网络分区时 etcd 选择一致性和分区容忍性，失去多数派的一侧不能继续线性一致写入；这不等于客户端 watch 永远及时。

### 4.2 服务发现与 watch

**面试官：客户端如何发现节点上下线？**

**回答：**当前客户端先读取 /services/<service>/ 获取全量节点，再启动 watch 监听同一前缀。PUT 将地址加入节点集和哈希环；DELETE 同时移除节点、虚拟节点、breaker 和 Channel。更新由 nodes_mutex_ 保护，因此请求线程不会看到节点集和路由更新到一半的状态。

连环追问与回答：

- **客户端启动时如何拉取节点列表？**
  FetchAllServices 同步读取服务前缀，解析 key 尾部地址并构建本地哈希环。
- **watch 监听哪些 key？**
  监听 /services/<service_name>/ 前缀下的 PUT/DELETE。
- **watch 事件丢失怎么办？**
  当前实现没有完整 revision/compaction 恢复，这是明确局限。生产上应记录 list revision，从该 revision watch；断连或 compact 后重新全量 list 再续 watch。
- **etcd 暂时不可用，客户端还能不能用旧节点列表？**
  已运行客户端可以短期使用旧视图；新客户端可能无法启动发现，节点上下线也无法及时感知。旧视图是可用性兜底，不是永远正确。
- **节点频繁上下线会不会导致抖动？**
  会反复重建路由和造成缓存冷启动。生产上需 readiness、去抖、最短摘除时间和主动健康检查。
- **客户端更新节点列表时，如何同步更新一致性哈希环？**
  当前 watch 回调持 nodes_mutex_，在同一临界区内更新 cache_nodes_ 和 ConsistentHashMap。哈希环内部还用 shared_mutex 保证查询和增删互斥。

**延迟视图的边界：**如果 A 已收到 DELETE、B 延迟、C 漏掉，B/C 仍可能访问旧节点。当前只靠 RPC deadline 和 per-node breaker 快速失败，不会立即把不可达节点从哈希环临时摘除，也不会自动重试后继节点。生产上应分开维护 etcd 视图与健康视图，对读请求做有限重路由；写请求必须结合幂等和版本语义。

项目重点：
- 要说明“旧节点列表可作为短期兜底”，否则 etcd 短暂故障会导致系统不可用。
- 哈希环更新要注意线程安全。

## 5. 一致性哈希与虚拟节点

### 5.1 一致性哈希原理

**面试官：什么是一致性哈希？为什么缓存系统需要它？**

**回答：**一致性哈希把节点和 key 映射到同一个环形哈希空间，key 归属于顺时针遇到的第一个节点。它的一致性是节点集合变化时，大部分 key 的映射仍保持不变，不是缓存数据强一致。缓存需要它，是为了扩缩容或节点故障时避免普通取模导致大面积 key 重映射和集中回源。

连环追问与回答：

- **普通 hash 取模有什么问题？**
  hash(key) % N 的 N 一变，大多数余数都会变化，导致大部分缓存同时失效。
- **节点增加或删除时，哪些 key 会受影响？**
  只影响该节点在环上接管或释放的相邻区间；理想均匀情况下约为总 key 的 1/N。
- **一致性哈希如何降低缓存失效范围？**
  节点变化只改变局部环区间的 owner，其他区间的顺时针后继不变。
- **哈希环顺时针查找是什么意思？**
  对 key hash 后，在有序虚拟节点数组中找第一个不小于 key hash 的点；到末尾就回到数组开头。
- **如果 key 落在两个节点之间，归属哪个节点？**
  归属顺时针方向的后一个虚拟节点对应的真实节点。
- **哈希环为空怎么办？**
  Get 返回空，客户端应快速返回无可用节点，不能对空容器取值。

八股延伸：
- **时间复杂度如何实现？** 当前查询使用二分查找，为 O(log V)，V 是虚拟节点数；新增后排序约 O(V log V)，删除当前实现还会线性移除。
- **用 map 还是排序数组？** 有序 map 增删方便，排序数组查询缓存局部性好。当前选择 vector + lower_bound + hash_map。
- **查找为什么用 lower_bound？** 它恰好返回第一个不小于 key hash 的虚拟节点，符合顺时针选择规则。

### 5.2 虚拟节点

**面试官：虚拟节点解决什么问题？**

**回答：**真实节点很少时，每个节点在环上只有一个点，区间长度方差很大，数据和请求容易倾斜。虚拟节点让每台物理机在环上出现多个位置，使其获得许多小区间，从统计上改善均匀性。它解决的是分布方差，不解决单热点 key。

连环追问与回答：

- **没有虚拟节点会出现什么问题？**
  少量随机点会切出大小差异很大的区间，部分节点承载远高于平均值的数据。
- **虚拟节点数量怎么设置？**
  需要按节点数、允许的负载偏差、建环成本和压测确定。当前默认每节点 10，最小 10、最大 200，数值偏实验性质。
- **虚拟节点越多越好吗？**
  不是。越多通常越均匀，但会增加内存、排序、删除、重平衡和路由元数据的 CPU/cache 成本，变更时也会制造更多映射扰动。
- **节点性能不同，如何做权重？**
  高容量节点分配更多虚拟节点，低容量节点更少；权重应来自稳定配置或容量指标，而不是只看历史请求数。
- **CRC32 作为哈希函数有什么优缺点？**
  优点是快、实现简单；缺点是 32 位、非加密、不可抵御恶意碰撞，分布质量也不如一些现代非加密哈希。
- **哈希冲突怎么处理？**
  生产上应让同一 hash 对应冲突桶，或加入节点 ID/二次排序确定归属。当前 unordered_map<uint32_t,string> 会被后写节点覆盖，是代码边界。

项目追问：
- **你的虚拟节点数是多少？** 默认 10，后台实验性重平衡范围是 10 到 200。
- **节点上下线时哈希环如何重建？** Add 生成 node-i 的 CRC32 并排序；Remove 根据记录的副本数删除该节点所有 hash。
- **重建期间请求如何处理？** Add/Remove 持 unique lock，Get 持 shared lock，因此请求会短暂等待写锁，不会读到半构建状态。

### 5.3 缓存失效范围

**面试官：你说一致性哈希降低节点上下线的缓存失效范围，具体怎么理解？**

**回答：**删除一个节点时，它负责的环区间由顺时针后继接管；新增节点时，它只接管自己逆时针前驱到自身之间的区间。其他区间的 owner 不变，所以不会像取模那样全局重排。这里迁移的是路由归属，当前项目没有搬运缓存数据，新 owner 第一次访问仍然要回源。

连环追问与回答：

- **N 个节点里删除 1 个节点，大约多少 key 受影响？** 理论均匀时约 1/N，实际取决于虚拟节点与 key 分布。
- **新增 1 个节点，大约多少 key 迁移？** 从原 N 个节点扩到 N+1，理想上新节点接管约 1/(N+1)。
- **这只是理论均匀情况下，对吗？** 对。虚拟节点少、hash 质量差、key 分布偏斜或权重不同都会偏离该比例。
- **虚拟节点如何让分布更接近均匀？** 每个物理节点分散占有多个小区间，单个异常大区间对总负载的影响被平均。
- **如果热点 key 全在一个节点上，一致性哈希能解决吗？** 不能。一个 key 只有一个 hash 和 owner；需要热点复制、key 拆分、请求合并或业务聚合。当前 SingleFlight 只能降低热点 miss 的回源数，不能提升该 key 的缓存命中并发。

**当前自适应重平衡的边界：**项目每秒根据请求计数检查倾斜并调整虚拟节点数。这是实验能力，请求数不等于 CPU/内存负载，而且改环不迁数据，会额外制造冷启动；生产中更适合稳定权重和渐进迁移。

## 6. LRU/TTL 本地缓存

### 6.1 LRU 结构

**面试官：你的 LRU 缓存怎么实现？**

**回答：**当前 LRU 使用双向链表加哈希表。链表头表示最近访问，尾部表示最久未访问；哈希表从 key 映射到链表迭代器。Get 命中后更新最近访问顺序，Set 插入或覆盖，按 key 和 value 的字节数累计容量，超过 max_bytes 时从尾部淘汰。整个结构由一把 mutex 保护。

连环追问与回答：

- **为什么 list + unordered_map 可以 O(1)？**
  unordered_map 平均 O(1) 定位链表迭代器，list 已知迭代器后可 O(1) 删除和头插；单独使用 list 查找是 O(N)，单独使用 map 又无法维护淘汰顺序。
- **get 操作为什么要移动节点？**
  精确 LRU 要把刚访问的 entry 标记为最新使用，否则淘汰顺序只反映写入时间，不反映读取热度。
- **put 已存在 key 怎么处理？**
  当前删除旧链表节点、修正 bytes，再把新值插到头部并更新 map 迭代器。TTL 也以本次写入重新计算。
- **容量满了淘汰哪个节点？**
  淘汰链表尾部，也就是最久未使用 entry；如果一次写入仍超限，会循环淘汰直到不超过字节预算。
- **删除节点时 map 和 list 如何同步？**
  必须在同一把锁下完成，并先保存后续需要的 key/value，避免 list erase 后再访问悬空对象。两份结构任意一份漏删都会造成悬空迭代器或内存统计错误。
- **迭代器失效要注意什么？**
  list 删除当前节点只使该节点迭代器失效；但当前实现通过 erase 再 emplace_front，必须立即把 map 中迭代器更新为 list.begin。不能在 erase 后继续引用原 Entry。

手撕延伸：
- **手写 LRU Cache：**先保证 map/list 的不变量，再写 Get、Set、Delete、evict。
- **加线程安全：**最简单是整次复合操作持 mutex，不能只给 map 和 list 分别加锁。
- **加 TTL：**entry 保存 steady_clock 语义的绝对过期时间，Get 时检查；不要用系统墙上时间避免时间回拨。
- **加按字节容量限制：**维护 bytes，并在覆盖、删除、过期和淘汰所有路径上对称更新。

### 6.2 TTL

**面试官：TTL 是怎么实现的？**

**回答：**entry 写入时保存 expire_at，ttl_ms 大于零时等于当前 steady_clock 时间加 TTL，零表示永不过期。Get 命中后先判断是否到期；到期就在锁内从 list、map 和 bytes 中删除并返回 miss。因此当前是 expire-after-write 加惰性过期，不是 expire-after-access，也没有 refresh-after-write。

连环追问与回答：

- **每个 entry 是否记录过期时间？**
  是，CacheEntry 的 expire_at_ms_ 保存绝对过期时间；移动 LRU 位置时必须保留原 TTL，不能因读取而续期。
- **过期检查在 get 时做，还是后台线程扫描？**
  当前只在 Get 时惰性检查，没有后台清理线程。
- **惰性删除有什么问题？**
  从不再访问的冷 key 即使已经过期，也会继续占用内存和容量统计，直到再次 Get 或因容量淘汰被清除。
- **定期扫描有什么问题？**
  会消耗 CPU、争用 LRU 锁，而且全量扫描可能制造周期性延迟尖峰。常见改进是分批扫描、时间轮或过期小根堆，但实现复杂度更高。
- **过期数据能否作为 fallback 返回？**
  fresh cache 的过期 entry 会被删除，不能直接返回。项目另外维护 stale cache，只有回源失败或 breaker Open 时才作为降级读取，并应有独立陈旧上限。
- **TTL 太长或太短分别有什么影响？**
  太长会扩大旧数据和失效广播失败的不一致窗口；太短会降低命中率并增加回源、击穿和下游压力。

**三种语义的取舍：**expire-after-write 控制写入后的最大新鲜度窗口，最符合当前缓存一致性语义；expire-after-access 适合会话等“活跃即保留”的数据，但热点旧值可能长期不失效；refresh-after-write 在到期前异步刷新，可降低热点尖峰，但需要独立的刷新 SingleFlight、失败策略和旧值上限。

### 6.3 线程安全

**面试官：线程安全 LRU 如何加锁？**

**回答：**当前 Get、Set、Delete 和容量查询都使用同一把 mutex。对精确 LRU 来说，Get 不只是读取 map，它还会改变链表顺序，因此必须按写操作保护。这个方案正确且简单，但每次命中都经过同一临界区，高并发扩展性受到限制。

连环追问与回答：

- **get 是读操作还是写操作？**
  对 value 内容是读，对 LRU 元数据是写。只要命中后更新顺序，整体就是写操作。
- **为什么 get 也可能需要写锁？**
  因为需要删除原链表位置、插到头部并更新 map 中的迭代器，这些都是复合修改。
- **多线程同时 get 同一个 key 会怎样？**
  当前会在 mutex 上串行，每个线程依次完成查找、移动和复制。不会破坏结构，但单热点 key 的锁交接成本很高。
- **全局锁有什么性能问题？**
  不同 key 也会互相阻塞；锁所在 cache line 在 CPU 核间反复转移，并引发 futex 唤醒、等待和上下文调度。
- **如何用分片锁优化？**
  按 hash(key) 选择 shard，每个 shard 有独立 map、list、mutex 和容量预算。多 key 请求可分散到不同锁，但同一 key 始终在一个 shard，无法消除单热点竞争。
- **value 很大时，锁内拷贝会不会影响性能？**
  会。当前 Get 在锁内复制 ByteView 所持数据，值越大临界区越长。可评估共享不可变 value、引用计数或在保证生命周期后缩小锁内复制，但要避免悬空引用。

项目追问：
- **你是否做了分片锁？** 当前没有，仍是一组 cache 一把 mutex。
- **如果没有，当前瓶颈在哪里？** 64 线程裸测中，KCacheGroup::Get 到 pthread_mutex_unlock/futex_wake 是主要调用路径，说明频繁锁交接是首要代码热点。
- **140W 裸测是否说明锁不是瓶颈？** 不能。绝对 QPS 高不代表扩展性好；单线程约 5.04M QPS，64 线程反而约 1.47M QPS，说明增加线程后同步和调度成本压过并行收益。

**优化排序：**多 key 场景优先做分片；然后评估 Clock 或 Redis 式采样近似 LRU，减少每次命中的精确链表写。shared_mutex 对当前精确 LRU 收益有限；TinyLFU 主要改善准入和命中率，不直接解决锁；无锁 LRU 的链表、哈希和内存回收复杂度暂不值得投入。

## 7. SingleFlight 防击穿

### 7.1 基本原理

**面试官：SingleFlight 是什么？解决什么问题？**

**回答：**SingleFlight 是按 key 合并进行中回源的并发协调机制。fresh cache miss 后，第一个进入的请求成为 pioneer，在 inflight map 中创建 Call 并执行 LoadData；同 key 后续请求只拿到同一个 Call 的 shared_future 等待结果。这样热点 key 失效时，多个并发请求通常只产生一次真实 getter 调用。

连环追问与回答：

- **缓存击穿是什么？**
  某个高频热点 key 失效后，大量并发请求同时 miss 并直接打到数据库，使后端瞬间过载。
- **SingleFlight 如何合并同 key 并发回源？**
  mutex 保护 key 到 Call 的 map。pioneer 先登记 Call 后释放锁并回源；waiter 找到已有 Call，复制 shared_ptr 后释放锁，等待其中的 shared_future。
- **它和互斥锁有什么区别？**
  普通全局锁只是串行化，后续线程拿到锁后仍可能再次回源；SingleFlight 既按 key 隔离，又共享 pioneer 的结果。它内部仍使用 mutex，但锁只保护 Call 的登记和删除。
- **不同 key 会不会互相阻塞？**
  只会在极短的 map 操作上竞争同一 mutex；实际 getter 和 future 等待都在锁外，所以不同 key 可以并行回源。
- **第一个请求叫 leader，其他请求怎么等待？**
  项目称其为 pioneer；waiter 对 Call 的 shared_future 执行 wait_for/get，因此多个线程可以读取同一异步结果。
- **leader 完成后如何唤醒等待者？**
  pioneer 对 promise set_value 或 set_exception，共享状态变为 ready，等待同一 shared_future 的线程被唤醒。

### 7.2 异常场景

**面试官：如果 leader 回源失败，等待者拿到什么？**

**回答：**pioneer 会把统一的 SingleFlightResult 或 exception 发布给 waiter，waiter 不会各自立即回源。结果区分 PioneerFailure、WaiterTimeout 和 CooldownRejected：只有代表真实底层失败的一次结果应该上报 breaker，不能让几十个 waiter 对同一次故障重复计数。

当前 Call 有 atomic failure_reported。一轮调用中，若 pioneer 先返回失败，由 pioneer 上报；若 pioneer 卡住，最先超时的 waiter 可以代表这一轮上报并把 key 加入失败冷却，其他 waiter 和迟到 pioneer 只降级，不重复累计 breaker。CircuitBreaker 的 generation permit 还会忽略状态已经转代后的旧结果。

连环追问与回答：

- **返回同一个错误还是每个等待者重试？**
  正常完成时所有 waiter 复用同一结果。失败时也统一降级；立即让每个 waiter 重试会重新形成击穿。
- **leader 超时怎么办？**
  LoadData 内 pioneer 等 getter future 到 getter_timeout，超时后返回 PioneerFailure、更新 timeout 指标并走 fallback。底层 detached worker 不会因此被取消。
- **等待者是否有自己的超时？**
  有。SingleFlight 的 wait_timeout 防止 waiter 永久挂在已有 Call 上；其数值当前复用 getter_timeout_ms。
- **SingleFlight map 什么时候删除 key？**
  pioneer 离开 Do 时由 RAII guard 删除自己的 Call；waiter 超时也可 Forget 旧 Call，让后续请求有机会重试。
- **如果删除太早会怎样？**
  旧 pioneer 仍在执行时，新请求会创建另一轮 pioneer，短暂出现同 key 多次回源。当前 waiter timeout 主动 Forget 是可用性优先的有意取舍。
- **如果删除太晚会怎样？**
  后续请求会一直加入已经卡死或已无价值的 Call，造成永久阻塞或持续降级。
- **回源很慢时，大量等待者会不会堆积？**
  会。SingleFlight 降低的是后端调用数，不会消灭等待请求本身；所以还需要 waiter deadline、限流、熔断和 stale-while-error。

**为什么使用 detach + promise，而不是 std::async：**std::async(launch::async) 返回的 future 即使 wait_for 超时，也不会取消 getter；而且当该 future 是异步共享状态最后一个引用时，析构可能等待任务结束。局部 future 在函数 return 前析构，因此代码虽然判定超时，调用线程仍可能卡住。当前第一层是 detached getter worker 通过 promise/future 向 pioneer 传结果；第二层是 pioneer 通过 SingleFlight promise/shared_future 向 waiter 广播。这样能让请求线程按时返回，但不能停止卡死 worker。

**线程泄漏边界：**C++ 没有安全的通用强杀线程接口，jthread stop_token 也是协作式。线程池只能限定永久卡死线程的数量，所有 worker 卡住后池仍会耗尽。生产上必须把 deadline/cancel 传到底层数据库、HTTP 或 RPC；无法取消的第三方调用应隔离到独立进程，由 watchdog 超时终止并重建。当前 fail cooldown 和 breaker 只是抑制灾难扩散，不是根治。

### 7.3 和穿透、雪崩的区别

**面试官：SingleFlight 能解决缓存穿透吗？**

**回答：**不能完整解决。SingleFlight 主要处理同一个热点 key 在同一时间窗口内的缓存击穿。持续请求不同的不存在 key，或者同一不存在 key 在每轮 SingleFlight 结束后再次请求，仍会访问后端，需要空值缓存或布隆过滤器。大量不同 key 同时过期属于雪崩，需要 TTL 抖动、预热、限流和多级缓存。

连环追问与回答：

- **缓存穿透是什么？**
  请求的数据在缓存和后端都不存在，导致请求长期绕过缓存访问后端；恶意随机 key 尤其危险。
- **空值缓存能解决什么？**
  将“不存在”作为短 TTL 结果缓存，避免同一不存在 key 重复查库。要区分 clean nullopt 和真实 getter 故障，前者不应触发 breaker。
- **布隆过滤器能解决什么？**
  在访问缓存/数据库前快速判断 key 一定不存在，节省下游访问；它可能误判存在，不能支持可靠删除时还需计数布隆等变体。
- **缓存雪崩是什么？**
  大量不同 key 同时过期或整个缓存层不可用，使回源流量同时冲击后端。
- **TTL 加随机扰动有什么作用？**
  将同一批写入的过期时间打散，降低某一时刻集中 miss；它不能解决单热点 key。
- **SingleFlight 主要解决哪一种问题？**
  主要解决缓存击穿，具体是同 key 并发回源合并。

标准回答：
- SingleFlight 主要缓解热点 key 过期后的缓存击穿。
- 对穿透需要空值缓存或布隆过滤器。
- 对雪崩需要 TTL 随机化、预热、多级缓存、限流降级。

## 8. 回源、降级与过期缓存

### 8.1 回源流程

**面试官：缓存 miss 后怎么回源？**

**回答：**fresh LRU miss 后，KCacheGroup::Load 先向服务端 breaker 申请 permit，再调用 SingleFlight::Do。pioneer 的 LoadData 启动 detached worker 执行 DataGetter，并通过 promise/future 等待结果；成功后写 fresh cache 和 stale cache，失败或超时返回带错误来源的 SingleFlightResult，再由 Load 记录 breaker 并进入 Fallback。

连环追问与回答：

- **回源接口是同步还是异步？**
  DataGetter 的接口语义是同步函数，但当前为了实现请求级超时，将它放进 detached worker 执行。它不是 gRPC CompletionQueue 异步，也没有真正可取消的异步 I/O。
- **回源超时如何设置？**
  getter_timeout_ms 控制 pioneer 等待 worker 的最长时间，同时也作为 waiter 等待已有 SingleFlight Call 的超时。超时是等待方放弃，不是杀死 getter。
- **回源成功后如何写缓存？**
  写入 fresh LRU 时使用 cache_ttl_ms，写 stale LRU 时使用 stale_ttl_ms；随后把成功结果发布给 waiter，并用 breaker permit 记录成功。
- **回源失败是否缓存失败结果？**
  真故障不会写 fresh cache，会进入失败 cooldown 并走降级。正常的“数据不存在”是 clean nullopt，不应被当作故障；当前也没有正式的负缓存，生产可为不存在值设置短 TTL。
- **后端数据源慢时，线程会不会堆积？**
  会。SingleFlight 只减少同 key 的真实 getter 数，waiter 和不同 key 的 worker 仍会占资源；detach 还可能留下超时后继续运行的线程。
- **服务端工作线程数如何限制？**
  gRPC Server 通过 ResourceQuota/线程配置限制框架资源，但这不直接限制 detached getter 数。生产需要专门的有界回源执行器、队列、per-backend bulkhead 和可取消 I/O。

### 8.2 过期缓存降级

**面试官：回源失败时为什么可以返回过期缓存？**

**回答：**这是可用性优先的降级选择。当后端短暂故障时，一个几秒或几分钟旧的数据可能比完全失败更有业务价值。项目没有直接返回 fresh cache 中已经 TTL 过期的 entry，而是维护独立 stale cache：正常回源/Set 时同步写入，只有 breaker Open 或真实回源失败时读取。

连环追问与回答：

- **过期缓存是否可能是脏数据？**
  一定存在这种可能，因此它只能被称为 stale，而不能伪装成强一致的新值。
- **哪些业务可以接受？**
  推荐列表、商品展示、非关键配置、统计数据等允许短时陈旧读的场景。
- **哪些业务不能接受？**
  余额、库存扣减、权限判断、交易状态等正确性高于可用性的场景。
- **返回过期缓存时是否要标记 degraded？**
  应该。当前内部增加 fallback_hits 并写 warning 日志，但 RPC response 没有完整 degraded 元数据；生产接口应显式标记来源和数据年龄。
- **过期多久以内可以返回？**
  由业务 SLO 决定，不应统一拍脑袋。项目通过 stale_ttl_ms 设置独立上限，零表示不自动过期，生产环境通常不应配置为无限。
- **如何避免一直返回很老的数据？**
  设置 stale TTL、后台恢复探测和告警；breaker 恢复后重新回源。还可在 response 中携带版本/时间戳，让调用方决定是否接受。

### 8.3 fallback

**面试官：fallback 返回什么？**

**回答：**Fallback 是回源不可用时的最后处理链。当前先尝试 stale cache，再调用用户传入的 FallbackGetter；两者都没有结果才返回 nullopt。FallbackGetter 可以返回默认值或备用数据源结果，具体由业务定义。

连环追问与回答：

- **fallback 和过期缓存降级有什么区别？**
  stale cache 是该 key 曾经成功加载的历史值；FallbackGetter 是另一套业务兜底逻辑，可能是默认值、静态配置或备用数据源。
- **fallback 是默认值、错误码还是兜底数据？**
  当前类型是 optional ByteView，所以可返回兜底数据或空；没有细分错误码和 degraded 元信息，是接口可演进点。
- **fallback 是否会污染缓存？**
  当前不会写回 fresh cache，这是正确的保守策略。否则临时默认值可能在后端恢复后继续被当成正常数据命中。
- **fallback 是否计入监控指标？**
  stale 或 FallbackGetter 返回有效值时会增加 fallback_hits；无 fallback 的失败只记日志，生产指标可进一步拆分 stale_hit、fallback_hit 和 fallback_miss。
- **fallback 太多说明什么问题？**
  说明后端错误/超时、breaker 长期 Open、TTL/容量设置导致 miss 过多，或者缓存预热不足，应结合 loader_errors、getter_timeouts 和 hit ratio 判断。

## 9. 熔断机制

### 9.1 客户端 per-node 熔断

**面试官：为什么客户端要维护 per-node 熔断器？**

**回答：**缓存节点故障通常是局部的。客户端按地址维护 breaker，某个节点连续 RPC 失败时只隔离这个地址，健康节点仍可被访问。全局 breaker 会因为一台机器故障误伤整个集群。当前客户端 breaker 主要提供快速失败，并没有实现自动重选后继节点。

连环追问与回答：

- **如果只做全局熔断会怎样？**
  任意节点故障都累计到同一阈值，最终把本可正常服务的节点一起拒绝，扩大故障域。
- **单个节点故障时，如何精准隔离？**
  breaker_pool_ 以 addr 为 key；RPC 成功/失败只更新对应节点状态，watch DELETE 时删除该 breaker。
- **熔断后请求是否换节点？**
  当前不换，直接失败或由上层处理。生产读路径可在健康哈希环选择后继并只重试一次，防止重试风暴。
- **换节点会不会破坏一致性哈希？**
  会偏离原 owner，可能读到旧副本并制造多节点冗余；写请求还涉及重复写和顺序。因此容灾路由必须配合版本、幂等和副本语义。
- **如果 key 按哈希就该落到故障节点，降级策略是什么？**
  当前是快速返回失败；如果服务端已收到请求但后端故障，则由服务端 stale/fallback 降级。客户端透明换节点尚未实现。
- **per-node 熔断状态如何线程安全维护？**
  breaker_pool_ 由 breaker_mutex_ 保护，每个 CircuitBreaker 内部状态又由自身 mutex 保护；RPC 路径复制 shared_ptr 后释放池锁。

### 9.2 服务端熔断

**面试官：服务端熔断保护什么？**

**回答：**服务端 breaker 保护 DataGetter 和数据库等后端依赖，避免 cache miss 在后端持续故障时不断创建回源。它位于 fresh cache miss 之后，所以本地命中仍能正常返回。客户端 breaker 保护的是调用客户端到某个缓存节点的 RPC 路径，两层保护的故障域不同。

连环追问与回答：

- **服务端熔断和客户端熔断有什么区别？**
  客户端按缓存节点地址隔离网络/节点故障；服务端按 Group 隔离后端 getter 故障。
- **服务端熔断保护后端数据源，对吗？**
  对，主要目标是防止数据库慢或失败时缓存层继续放大压力。
- **回源失败多少次触发？**
  默认 failure_threshold 为 5，并受 failure_reset_timeout_ms 窗口影响；参数应按后端 SLO 调整。
- **熔断后是返回过期缓存还是错误？**
  Open 时直接调用 Fallback，优先 stale cache，其次 FallbackGetter，均无结果才返回空。
- **服务端熔断是否影响缓存命中请求？**
  不影响。Get 先查 fresh LRU，miss 才进入 Load 和 breaker。

### 9.3 三态状态机

**面试官：三态熔断状态如何转移？**

**回答：**Closed 正常放行并统计失败；失败达到阈值转 Open。Open 在恢复时间内直接拒绝，时间到后转 HalfOpen。HalfOpen 只允许少量探测，满足成功阈值才回 Closed，任一探测失败立即回 Open。状态和计数由 mutex 保护。

连环追问与回答：

- **closed、open、half-open 分别是什么意思？**
  Closed 是正常服务；Open 是已判定依赖故障、快速失败；HalfOpen 是恢复探测阶段。
- **open 状态持续多久？**
  默认 recovery_timeout_ms 为 5000 ms，之后下一次 Allow 触发 HalfOpen。
- **half-open 允许几个探测请求？**
  默认 half_open_max_calls 为 2，避免恢复中的后端立刻承受全量流量。
- **探测成功几次恢复？**
  默认 success_threshold 为 2，并要求已完成探测满足状态判断。
- **探测失败怎么办？**
  立即重新 Open，刷新 open 时间并清空 HalfOpen 计数。
- **失败计数窗口怎么设计？**
  当前用最近失败时间和 failure_reset_timeout_ms 做衰减，不是完整滑动桶。窗口内成功不会简单清零，避免失败/成功交替永远无法熔断。

**迟到请求问题：**Allow 返回 generation permit；RecordSuccess、RecordFailure 和 Cancel 只接受当前 generation。这样状态已经 Open/HalfOpen/Closed 转代后，旧 in-flight 结果不会回头污染新状态。

## 10. 缓存一致性与失效通知

### 10.1 节点缓存冗余

**面试官：你提到节点缓存冗余，为什么会有冗余？**

**回答：**在某一时刻、某个客户端视图中，一个 key 确实只有一个 owner；但不同客户端 watch 延迟、哈希环变化、旧节点未清理以及 stale cache 会让同一个 key 在多个节点留下副本。当前项目没有数据迁移或副本协议，因此冗余更多是最终一致性风险，而不是受控复制优化。

连环追问与回答：

- **一个 key 理论上不是只归属一个节点吗？**
  是，但这个结论依赖所有客户端拥有同一节点视图；路由版本不同就可能选出不同 owner。
- **客户端路由变化会不会造成冗余？**
  会。新 owner 回源并缓存后，旧 owner 的 entry 不会因为哈希环变化自动删除。
- **节点上下线后旧节点缓存如何处理？**
  当前不迁移、不主动清空；容量淘汰、TTL 或失效广播最终删除。节点重新加入还可能带着旧进程内数据，实际部署应重启清空或校验版本。
- **多节点同时缓存同一个 key 会有什么问题？**
  写入后可能读到旧副本，失效广播也出现写放大、失败和乱序问题。
- **冗余是性能优化还是一致性风险？**
  当前主要是风险。只有在有明确副本放置、版本和读写协议时，冗余才能成为容灾或就近读取优化。

### 10.2 写入/删除失效通知

**面试官：写入/删除时的失效通知怎么设计？**

**回答：**Set 先由客户端按一致性哈希写目标节点，再由同一个 Client SDK 向其他已知节点发送 Invalidate；Delete 则向所有已知节点广播 Delete。接收 Invalidate 的服务端只删除本地 fresh/stale cache，不再次传播，避免循环。该流程不是事务，广播失败不会回滚已成功的目标写入，因此只能称为最终一致。

连环追问与回答：

- **谁发通知？**
  当前由发起 Set/Delete 的 Client SDK，而不是缓存节点或数据库变更日志。
- **通知发给哪些节点？**
  Set 对目标节点之外的已知节点发 Invalidate；Delete 对所有已知存活节点发 Delete。
- **通过 gRPC 还是消息队列？**
  当前是同步 Unary gRPC，广播串行执行。没有可靠消息队列和持久化重试。
- **通知失败怎么办？**
  当前汇总失败并返回 false，但已成功操作不回滚，主要依赖 TTL。生产应把失败任务写入可靠队列重试，并保证幂等。
- **通知乱序怎么办？**
  当前没有版本控制。生产应为值和失效事件携带单调版本/逻辑时间，节点只接受不旧于本地版本的操作。
- **TTL 兜底意味着最大不一致窗口是多少？**
  对带 cache TTL 的回源数据，理论上不超过剩余 TTL；但主动 Set 当前调用无 TTL 重载，可能永不过期，所以不能声称系统总有严格上界。这是当前实现的重要边界。

**额外性能隐患：**当前广播期间持有 nodes_mutex_ 且串行做 RPC，会阻塞 watch 和其他路由操作。应先在锁内复制节点快照，释放锁后用有界并发广播。

校招建议：
- 不要把这个讲成强一致缓存。
- 更合理的说法是“通过主动失效缩短不一致窗口，TTL 作为最终兜底”。

## 11. Prometheus 指标

### 11.1 指标类型

**面试官：Prometheus 有哪些指标类型？**

**回答：**Prometheus 的核心类型是 Counter、Gauge、Histogram 和 Summary。选择指标不是看数据名称，而是看状态语义：累计事件用 Counter，瞬时状态用 Gauge，需跨实例聚合的分布用 Histogram，客户端直接计算分位数才考虑 Summary。

连环追问与回答：

- **counter、gauge、histogram、summary 区别是什么？**
  Counter 单调递增，进程重启归零；Gauge 可增可减；Histogram 将样本累计到 bucket 并同时输出 count/sum；Summary 在客户端维护分位数和 count/sum，但分位数不便跨实例聚合。
- **QPS 用什么指标？**
  请求总数用 Counter，查询时用 rate 或 irate 计算每秒增量，不应直接维护一个易抖动的 QPS Gauge。
- **当前连接数用什么指标？**
  Gauge，因为连接会建立也会关闭。
- **延迟分位数用什么指标？**
  服务端集群通常用 Histogram，bucket 应围绕业务 SLO 设计；Summary 更适合单实例且不需聚合的场景。
- **P99 怎么计算？**
  对 Histogram 用 histogram_quantile(0.99, sum by(le)(rate(bucket[window]))) 近似，精度取决于 bucket 边界。
- **label 维度过多有什么问题？**
  每种 label 组合都是独立时序。把 key、user ID、request ID 放进 label 会造成高基数，增加内存、磁盘和查询开销。

### 11.2 项目指标

**面试官：你的项目暴露了哪些指标？**

**回答：**KCacheServer 用 cpp-httplib 暴露 Prometheus 文本格式。当前实际指标包括 local_hits、local_misses、hit_ratio、loader_hits、loader_errors、avg_load_duration_ms、circuit_breaks、fallback_hits、getter_timeouts、circuit_breaker_state、cache_bytes、cache_max_bytes 和 cache_count。它覆盖了缓存和回源状态，但还不是完整生产观测体系。

连环追问与回答：

- **cache hit/miss 怎么统计？**
  KCacheGroup::Get 在 fresh LRU 命中时增加 local_hits，未命中时增加 local_misses，hit_ratio 由两者计算。
- **SingleFlight 合并次数怎么统计？**
  当前没有独立指标。应在发现已有 Call 时增加 coalesced_waiters，并暴露 inflight Calls Gauge；不能用 loads 直接代替。
- **回源耗时怎么统计？**
  当前累加 Load 整体耗时并输出平均值，但 loads 包含进入 Load 的 waiter，不严格等于 getter 调用。应拆 actual_getter_duration Histogram、getter_invocations Counter 和 load_requests Counter。
- **熔断状态怎么暴露？**
  当前用 Gauge 数字表示 Closed=1、Open=2、HalfOpen=3，并用 circuit_breaks Counter 记录触发次数。
- **fallback 次数怎么统计？**
  当前 stale 或 fallback getter 成功都会增加 fallback_hits，建议拆成不同来源和 fallback miss。
- **gRPC 请求延迟怎么统计？**
  当前没有业务级 gRPC latency Histogram。应在 server interceptor 或 RPC handler 周围记录 method/status/latency，method 和 status 是有限 label。
- **如何通过指标判断缓存击穿？**
  观察 local_miss、actual getter、SingleFlight waiter/inflight、回源延迟、timeout、breaker 和 fallback 是否同时上升。只有 miss 上升不能证明击穿。

建议准备的指标：
- 请求总数。
- 命中率。
- 回源次数和耗时。
- SingleFlight inflight 数。
- 熔断打开次数。
- fallback 次数。
- gRPC 延迟 histogram。

## 12. ghz 压测与性能分析

### 12.1 压测指标解释

**面试官：你说 128 并发 QPS 1.88w、P99 14.79ms，怎么测的？**

**回答：**最新保留报告的口径是：单 VMware 虚拟机、8 vCPU、loopback gRPC，ghz 0.117.0，对单节点执行本地缓存命中的同步 Unary Get；128 并发、8 连接。无 perf 时为 19,342.23 QPS、P99 14.09 ms；perf 采样下为 16,926.02 QPS、P99 15.58 ms。清单中的 1.88w/14.79 ms 是旧轮次口径，面试应统一引用最新报告和对应命令，不能混报。

连环追问与回答：

- **ghz 命令参数是什么？**
  核心参数是目标 RPC、请求数据、总请求或持续时间、concurrency=128、connections=8。具体命令应从保留脚本/结果复述，不应凭记忆补造。
- **测的是热点读还是随机读？**
  全链路 perf 场景是固定已预热的本地缓存命中，重点测 RPC 命中路径；评估缓存算法还应补 keyspace=1 与多 key 均匀负载。
- **是否全部命中缓存？**
  目标 workload 是命中路径。结果仍有少量 Canceled/Unavailable，主要与 ghz 结束窗口关闭连接有关，报告未将其直接认定为业务错误。
- **是否经过 gRPC、Protobuf、网络、业务逻辑？**
  都经过，包括 loopback TCP、HTTP/2、Protobuf、gRPC 同步调度、KCacheServer 和 LRU Get；只是不经过物理网络。
- **VM 8C8G 具体配置是什么？**
  报告确认 8 vCPU 和 VMware 单机；若 8G 内存没有从原始环境重新确认，不应把标题口径当成精确硬件证明。
- **平均延迟是多少？**
  应从对应 ghz 原始结果读取；当前性能报告重点保留 QPS 和 P99，没有列出平均值时不要现场编造。
- **CPU 是否打满？**
  需要结合该轮 CPU 利用率或 perf stat 回答。perf 显示 CPU 时间主要落在 futex、gRPC 调度和 socket，不等于仅凭火焰图就能断言所有核 100%。
- **压测持续时间多久？**
  perf 轮产生约 101 万 OK 请求，具体持续时间应引用脚本参数。没有原始命令时应诚实说明报告未在摘要表中列出。

**4/8 连接与 128 并发：**连接数是 ghz 建立并复用的 HTTP/2/TCP transport 数；并发数是同时在途 RPC 数。128 个并发可以作为多个 stream 分摊到少数连接，它们不是“128 个线程”或“128 条 TCP 连接”。

必须说清：
- 单节点热点读全链路压测和裸测不是同一条链路。
- 最新约 19.3K QPS 的全链路结果更能反映 gRPC 服务实际链路；1.88w 是旧轮次口径。
- 140W 裸测只说明本地缓存结构本身上限较高。

### 12.2 裸测 140W QPS

**面试官：裸测 140W QPS 是怎么测的？为什么比 gRPC 高这么多？**

**回答：**bench_cache 直接调用 KCacheGroup 的本地缓存命中路径，用于隔离核心数据结构，不经过 SDK、gRPC、Protobuf、HTTP/2 和 socket。最新 64 线程无 perf 为 1,472,930 QPS，单线程约 5.04M QPS；所以“140W”是量级描述。它比全链路高，是因为去掉了 RPC 框架和网络栈，但它也暴露出线程越多反而吞吐下降的 LRU 锁竞争。

连环追问与回答：

- **裸测是否绕过网络和 gRPC？** 是，直接进程内调用。
- **是否只测 LRU get？** 测 KCacheGroup::Get 的命中路径，包含参数检查、状态计数和 LRU Get，不是只调用 unordered_map::find。
- **是否包含锁？** 包含 LRU 的单把 mutex，这正是高线程档的主要热点。
- **是否包含 Protobuf 序列化？** 不包含。
- **是否包含线程调度？** 包含 benchmark 多 worker 的操作系统调度、上下文切换和锁等待/唤醒。
- **这个指标的意义是什么？** 用于建立业务核心上限、判断数据结构扩展性并与全链路隔离归因，不能作为对外服务 QPS 宣传。

**除锁外的下降因素：**还包括核间 cache line 一致性、L1/L2/L3 命中率下降、线程迁移、上下文切换、调度器开销、哈希表局部性、字符串/value 复制和可能的 NUMA。应配合 perf stat 的 context-switch、cache-miss 等计数及分片前后对照验证。

建议回答：
- 裸测用于验证本地缓存核心数据结构不是主要瓶颈。
- gRPC 全链路包含网络、序列化、HTTP/2、线程调度、服务框架和业务逻辑，所以 QPS 明显低。

### 12.3 perf + 火焰图

**面试官：你如何用 perf + 火焰图定位瓶颈？**

**回答：**先分别设计全链路和裸调用两个 workload，再采无 perf 基线，随后以相同负载用 perf 采样调用栈。全链路主要落在 futex_wake、sendmsg/recvmsg 和 gRPC combiner/server call，KCache 函数不是显著热点；裸测则集中在 KCacheGroup::Get 到 mutex unlock/wait 和 futex 路径。正是调用者不同，才得出“全链路先优化 gRPC，同一进程核心先优化 LRU 锁”两个互不替代的结论。

连环追问与回答：

- **perf 采样原理是什么？**
  周期性中断采集当前指令和调用栈，用样本比例近似 CPU 时间分布。它是统计采样，不是每次调用的精确计时。
- **火焰图横轴和纵轴代表什么？**
  横向宽度代表累计样本量，纵向代表调用栈深度；横轴位置没有时间顺序意义。
- **你看到过哪些热点？**
  全链路约 49.84% Children 在 futex_wake、8.19% sendmsg、4.35% recvmsg；裸测约 71.04% 调用树经过 unlock 到 futex_wake，13.42% 经过 lock wait 到 futex_wait。
- **如何判断热点在业务代码还是框架代码？**
  看完整调用链的上层 caller。全链路 futex 的上游是 gRPC combiner/server 调度；裸测 futex 的上游是 KCacheGroup::Get/LRU mutex。
- **如果热点在锁竞争，火焰图怎么看？**
  会看到业务函数下出现 pthread_mutex_lock/unlock、__lll_lock_wait、futex_wait/wake 的宽栈，并结合并发增长时 QPS 不升反降。
- **如果热点在序列化，如何优化？**
  应先看到 protobuf encode/decode、copy 或 HPACK 等明确热点，再减少字段/消息大小、避免多余拷贝、复用 buffer 或做 BatchGet；当前报告不支持“序列化是主瓶颈”的结论。

**为什么 futex_wake 高于 futex_wait：**临界区短但交接频繁时，每次 unlock 都可能扫描并唤醒竞争者；部分线程自旋或快速获取，不一定长时间进入内核睡眠，因此 CPU 样本不会与 wait 对称。它表明锁所有权交接和调度很昂贵，不代表没有竞争。

## 13. 故障场景

### 13.1 缓存节点宕机

**面试官：一个缓存节点突然宕机，会发生什么？**

**回答：**节点进程宕机后不再续租，最长约一个 lease 周期后 etcd 删除注册 key；各客户端收到 DELETE 的时间可能不同。更新前仍会把部分 key 发到旧地址，RPC 由 deadline/KeepAlive 结束并计入该节点 breaker。更新哈希环后，原节点负责区间映射到顺时针后继；当前不迁移数据，所以这些 key 第一次访问新 owner 时会 miss 并回源。

连环追问与回答：

- **etcd 何时感知？** 当前 lease 为 10 秒，崩溃后在续租停止并到期时删除 key；不是毫秒级故障探测。
- **客户端 watch 何时更新？** etcd 产生 DELETE 后异步推送，各客户端因网络和调度可能先后不同。
- **更新前请求该节点会怎样？** 继续尝试旧 Channel，成功、连接失败或 deadline 超时；当前不会在首次不可达时立刻主动改环。
- **gRPC 调用失败是否触发客户端熔断？** 会对该地址 RecordFailure，达到阈值后快速拒绝；但不会自动换节点。
- **哈希环更新后，哪些 key 会迁移？** 原节点虚拟节点逆时针前驱到自身区间内的 key 改由对应后继负责。
- **被迁移 key 初次访问是否会回源？** 是，因为项目没有数据迁移和副本接管；会出现短期命中率下降。

### 13.2 etcd 不可用

**面试官：etcd 挂了，缓存系统还能工作吗？**

**回答：**数据面不依赖每次访问 etcd，所以已有客户端可使用内存中的旧节点表继续访问已知节点；但视图会逐渐陈旧。新客户端如果无法完成首次 list，就没有路由；节点变化也无法被感知。系统属于短期可用、长期风险上升，而不是“etcd 挂了完全无影响”。

连环追问与回答：

- **已有客户端是否能继续用旧节点列表？** 能，但请求已下线节点时依赖 deadline/breaker，且看不到新节点。
- **新客户端能不能启动？** 当前首次发现失败时无法构建有效路由，通常不能正常提供缓存访问。
- **节点上下线还能不能感知？** 不能，watch 中断期间事件不可依赖。
- **是否需要本地缓存服务列表？** 生产可持久化最近成功快照作为启动兜底，但必须标记 revision、更新时间和最大允许陈旧时间。
- **etcd 恢复后如何重新同步？** 应重新全量 list 获得新快照和 revision，再从该 revision watch；当前实现缺少完整重连/compact 恢复。

### 13.3 后端数据源变慢

**面试官：后端回源很慢，会发生什么？**

**回答：**同 key 的真实回源会被合并，但 pioneer worker 仍被慢 getter 占用，waiter 也在等待 shared_future；不同 key 还会各自产生 worker。getter timeout 让请求路径返回失败，waiter timeout 防止永远等待，失败 cooldown 抑制同 key 重试，breaker 在达到失败阈值后停止新回源，并转 stale/fallback。

连环追问与回答：

- **SingleFlight 会减少请求数，但等待线程会不会堆积？** 会，它降低下游请求数，不等于释放所有上游等待资源。
- **回源超时怎么设置？** 应小于调用方总 deadline，并根据数据库 SLO 分层配置；当前 getter_timeout 同时用于 pioneer 和 waiter，生产可拆开。
- **服务端熔断何时打开？** 默认窗口内 5 次代表真实回源的失败后 Open；waiter timeout 和 cooldown reject 不能重复计数。
- **是否返回过期缓存？** breaker Open 或回源真故障时优先 stale cache，前提是业务允许。
- **如何通过指标发现？** 看 avg/getter latency、getter_timeouts、loader_errors、fallback_hits、circuit_breaks 和线程/队列占用。
- **如何防止线程池被慢回源打满？** 有界线程池和队列只能做 bulkhead；根治要让底层 I/O 接受 deadline/cancel。无法取消的调用用进程隔离，否则所有 worker 最终仍可能被永久占满。

### 13.4 热点 key

**面试官：一个热点 key 突然过期，系统如何处理？**

**回答：**第一个 miss 成为 pioneer，其他同 key 请求作为 waiter 等它的 shared_future。当前正常路径是阻塞等待新值，而不是立即返回 stale；只有 waiter/pioneer 超时、回源失败或 breaker Open 后才走 stale/fallback。成功时 pioneer 先写缓存并 set promise，所有 waiter 读取同一结果。

连环追问与回答：

- **同 key 请求是否被 SingleFlight 合并？** 是，同一轮 Call 通常只执行一次 getter。
- **合并期间其他请求阻塞还是返回旧值？** 当前等待到 wait_timeout；没有实现 stale-while-revalidate 的立即旧值模式。
- **如果回源成功，所有等待者如何返回？** shared promise ready 后，waiter 的 shared_future 返回相同 ByteView。
- **如果回源失败，是否返回过期值？** 每个请求在 Load 结果失败后进入 Fallback，可读取 stale cache。
- **热点 key 是否应该提前刷新？** 对允许旧值的业务，refresh-ahead/stale-while-revalidate 能降低过期尖峰，但要用独立 SingleFlight 防止并发刷新，并设置最大陈旧时间。

## 14. 项目相关手撕题

### 14.1 LRU Cache

**面试官：手写 LRU Cache，要求 get/put O(1)。**

**回答：**使用 unordered_map<Key, list<Node>::iterator> 和双向链表。Get 查 map，命中后 splice 到头部；Put 已存在则更新并移头，不存在则头插；超容量时删除尾节点及其 map 项。核心不变量是 map 中每个迭代器都指向有效 list 节点，list 和 map 数量一致。

追问与回答：

- **加 TTL 怎么改？** Node 增加 expire_at，Get 命中先检查并删除过期节点；使用 steady_clock，所有删除路径同步维护容量。
- **加线程安全怎么改？** 一把 mutex 覆盖 map/list 的完整复合操作；不能查 map 后解锁再改 list。
- **加分片锁怎么改？** 外层数组保存多个独立 LRU shard，hash(key) 选 shard；总容量按均分或动态预算分配。
- **容量按字节怎么改？** Node 保存或计算 charge，Put 覆盖时先减旧值再加新值，循环淘汰到 bytes 不超过 max_bytes。

### 14.2 SingleFlight

**面试官：手写一个简化版 SingleFlight。**

**回答：**用 mutex 保护 unordered_map<Key, shared_ptr<Call>>；Call 内有 promise<Result> 和 shared_future<Result>。没有 Call 的线程创建并登记后成为 leader，释放锁执行函数；已有 Call 的线程复制 shared_ptr 后释放锁等待。leader 最终 set_value/set_exception，并按 key 和 Call 指针比较删除自身。

追问与回答：

- **如何记录 inflight key？** map 的 key 是业务 key，value 是共享 Call；必须先登记再执行函数，避免两个 leader 同时出现。
- **等待者如何阻塞等待？** shared_future.get 或 wait_for；等待期间不能持全局 map 锁。
- **leader 失败如何通知等待者？** set_exception 或设置统一错误 Result，否则 promise 被销毁会得到 broken_promise，语义不清。
- **如何避免 key 泄漏？** RAII finally guard 在所有正常/异常路径清理；删除时比较 Call 身份，避免旧 leader 删除新一轮。
- **多个 key 如何并发？** map 锁只保护短操作，实际 func 在锁外执行，因此各 key leader 并行。

### 14.3 一致性哈希

**面试官：实现一个一致性哈希环。**

**回答：**为每个物理节点生成 replicas 个虚拟节点标识 node-index，hash 后存入有序结构，并保存 hash 到物理节点的映射。Get 对 key hash 做 lower_bound，若到末尾则回环。并发实现中 Get 用 shared lock，Add/Remove 用 unique lock。

追问与回答：

- **添加节点怎么做？** 生成所有虚拟节点 hash，插入环并排序或维护树，同时记录物理节点到副本数的映射。
- **删除节点怎么做？** 根据该节点副本数重新生成对应 hash，从环和映射中全部删除。
- **查找 key 怎么做？** lower_bound 找第一个不小于 key hash 的虚拟节点，空环返回空。
- **虚拟节点怎么表示？** 可用 node + 分隔符 + index 的稳定字符串，所有客户端必须使用相同格式和哈希算法。
- **哈希冲突怎么处理？** 使用 hash 到候选列表、复合排序键或更宽 hash；不能像当前简单 map 一样静默覆盖而不说明。

### 14.4 阻塞队列或线程池

**面试官：写一个线程安全阻塞队列。**

**回答：**mutex 保护 queue、capacity 和 closed 状态，not_empty 与 not_full 两个 condition_variable 分别唤醒消费者和生产者。Push 在未满或已关闭谓词上等待，Pop 在非空或已关闭谓词上等待；操作队列后 notify 对应条件变量。

追问与回答：

- **条件变量为什么要 while 判断？** 存在虚假唤醒，而且多个线程被唤醒后条件可能已被别人消耗；wait 必须重新检查谓词。
- **如何关闭队列？** 持锁设置 closed=true，然后 notify_all；关闭后 Push 拒绝，Pop 可先取完剩余任务再返回关闭状态。
- **多生产者多消费者是否安全？** 只要所有队列和状态访问都在同一 mutex 下，并正确使用谓词等待，就是安全的。
- **队列满了怎么办？** 根据策略阻塞、超时返回或立即拒绝；缓存回源通常应有界并可快速拒绝，避免无限堆积。

## 15. 面试模拟追问链

### 链路 A：从分布式缓存架构到一致性哈希

1. **为什么需要分布式缓存？** 单机受容量、吞吐和故障域限制，分布式可以扩容，但需要额外解决路由和一致性。
2. **客户端如何知道有哪些节点？** 首次从 etcd 拉取服务前缀，之后使用 watch 维护本地节点快照。
3. **etcd watch 如何感知节点上下线？** 节点 PUT 表示注册，lease 到期产生 DELETE；生产还需 revision 和断线全量同步。
4. **key 如何路由到节点？** 客户端对 key 做 CRC32，在本地虚拟节点环 lower_bound 选择 owner。
5. **普通取模哈希有什么问题？** 节点数变化会使大多数 key 的余数改变，造成大面积缓存失效。
6. **一致性哈希如何降低迁移范围？** 只改变新增/删除节点相邻区间的归属。
7. **虚拟节点解决什么问题？** 将每台物理机分散到多个环点，降低少节点带来的区间倾斜。
8. **节点下线后缓存命中率会怎样？** 受影响区间没有数据迁移，会在新 owner 冷启动回源，命中率短暂下降。

### 链路 B：从缓存击穿到 SingleFlight

1. **缓存击穿是什么？** 热点 key 失效后同 key 并发同时回源，瞬间压垮后端。
2. **SingleFlight 如何解决？** 一个 pioneer 调 getter，其他 waiter 等同一个 shared_future。
3. **和互斥锁有什么区别？** 不仅互斥，还按 key 共享结果；不同 key 的 getter 可并发。
4. **leader 回源失败怎么办？** 发布统一失败，只由一名代表计 breaker，所有请求走 stale/fallback。
5. **等待者是否有超时？** 有，超时可 Forget 旧 Call，避免永久受卡住 pioneer 影响。
6. **回源很慢会不会线程堆积？** 会；SingleFlight 减少后端调用，不消除 waiter 和不同 key worker。
7. **如何配合过期缓存降级？** 回源真故障或 breaker Open 时读取独立 stale cache，并限制最大陈旧时间。

### 链路 C：从 gRPC 到性能

1. **gRPC 为什么使用 HTTP/2？** 它提供单连接多 stream、双向流、流控和 header 压缩。
2. **Protobuf 比 JSON 快在哪里？** 不传字段名、编码更紧凑并使用生成解析代码；收益仍要实测。
3. **Channel 为什么要复用？** 复用 TCP/HTTP2 和 gRPC 连接状态，避免每次握手和建连。
4. **stream 并发如何影响吞吐？** 过小会连接侧排队，过大可能把排队推到服务线程和业务层。
5. **全链路 QPS 怎么测的？** 最新口径是单 VM loopback、128 并发/8 连接、命中路径，约 19.3K QPS、P99 14.09 ms。
6. **裸测为什么高很多？** 它绕过 Protobuf、gRPC、HTTP2 和 socket，只保留进程内 KCache 命中路径。
7. **如果优化 P99 先看什么？** 先看同 workload 下排队位置和调用栈；当前全链路应先评估同步 gRPC 调度，裸测才优先 LRU 锁。

### 链路 D：从故障到熔断降级

1. **单个缓存节点故障怎么办？** deadline/节点 breaker 快速失败，watch DELETE 后改环；当前无透明后继重试。
2. **为什么客户端要 per-node 熔断？** 将局部节点故障限制在该地址，不误伤健康节点。
3. **服务端熔断保护什么？** 保护 miss 后的 DataGetter/数据库，fresh cache 命中不受影响。
4. **三态熔断如何转移？** Closed 达失败阈值转 Open，恢复时间后 HalfOpen，探测全成功 Close、失败则重新 Open。
5. **熔断后返回什么？** 优先 stale cache，再 fallback getter，均无则返回空/错误。
6. **过期缓存降级有什么风险？** 可能返回脏数据，必须按业务设置 stale TTL 并显式标记 degraded。
7. **Prometheus 如何观察降级是否频繁？** 联合观察 fallback_hits、circuit_breaks、getter_timeouts、loader_errors 和 hit ratio。

## 16. 这个项目最该提前准备的答案

- 3 分钟项目介绍。
- 一张架构图。
- key 查询完整链路。
- 节点上下线完整链路。
- 一致性哈希和虚拟节点解释。
- LRU/TTL 线程安全实现。
- SingleFlight 正常与异常流程。
- 客户端 per-node 熔断和服务端熔断区别。
- 过期缓存降级的数据一致性风险。
- ghz 压测范围、参数和指标解释。
- 裸测约 147W QPS 与 gRPC 全链路约 1.93W QPS 的差异解释，并说明旧轮次数字为何不同。

## 17. 第三方库与协议：应达到的工程理解层

这一节的目标不是阅读 gRPC、etcd 或 Protobuf 源码，而是能把“库/协议的职责”和“项目中的设计选择”对应起来。对未实际使用的异步 API、流式 RPC 等能力，应明确说明了解原理但未在本项目落地。

### 17.1 Protobuf

**面试官：为什么 gRPC 通常配合 Protobuf，而不是 JSON？**

**回答：**Protobuf 是强 schema 的二进制消息格式，gRPC 根据 proto 同时生成客户端 Stub、服务端接口和消息类型。内部服务重视契约、体积和解析效率，所以它比文本 JSON 更适合本项目；代价是可读性和临时调试体验较弱。Protobuf 只负责消息表示，HTTP/2/gRPC framing 才负责传输边界。

连环追问与回答：

- **proto 文件的作用是什么？代码是如何生成的？** 定义 message、字段号和 service RPC；protoc 加 gRPC 插件生成 pb.h/pb.cc 和 grpc.pb.h/grpc.pb.cc。
- **字段编号为什么不能随意修改或复用？** 线上二进制只携带字段号和 wire type，旧编号复用会让旧数据被解释成新语义。
- **optional、repeated、map、oneof 分别适合什么场景？** optional 区分未设置与默认值；repeated 表示列表；map 表示键值集合；oneof 表示互斥变体并节省同时存储。
- **新增字段、删除字段如何做到前后兼容？** 新增使用新编号且给默认语义；删除后 reserve 字段号和名称，避免复用；不要改变已有字段 wire type。
- **unknown field 的兼容语义是什么？** 旧端可跳过不认识的新字段，并通常在消息转发时保留，从而允许新旧版本共存。
- **Protobuf 为什么通常比 JSON 小、快？** 不传字段名，整数可用 varint，解析由生成代码按 schema 完成；但具体收益仍与字段和值分布有关。
- **Protobuf 能否解决消息边界问题？** 单独序列化格式不能解决 TCP 字节流边界；gRPC 在 HTTP/2 DATA/frame 上定义消息 framing。
- **schema 演进时为什么应保留已删除的字段编号？** 防止历史消息或旧客户端使用该编号时被新版本误解为另一字段。

建议回答：
- Protobuf 是二进制、强 schema 的序列化格式，字段编号和 varint 等编码减少传输体积与解析成本。
- 它不是加密格式，也不是自描述的通用业务协议；调试性通常不如 JSON。
- gRPC 通过 HTTP/2 frame 处理传输，Protobuf 主要处理消息序列化，两者职责不要混淆。

### 17.2 gRPC 调用模型

**面试官：你的缓存系统为什么选择同步 gRPC？同步、异步和回调 API 的差别是什么？**

**回答：**当前选择同步 Unary 是因为请求/响应模型简单，代码容易实现和验证，适合作为第一版。但性能报告表明同步 gRPC 的线程唤醒和 socket 调度占全链路主要 CPU，因此如果目标是端到端吞吐，CompletionQueue 或 Callback API 是比先改 LRU 更值得验证的方向。异步减少阻塞线程，不代表自动更快，状态机和对象生命周期错误也会抵消收益。

连环追问与回答：

- **四种 RPC 分别是什么，项目使用哪种？** Unary 是一问一答；server/client streaming 分别是一端发送流；bidirectional 两端都流式。当前 Get/Set/Delete/Invalidate 都是 Unary。
- **同步 RPC 阻塞的是哪个线程？** 客户端调用 Stub 的线程等待响应；服务端同步 API 由 gRPC 工作线程执行 handler，handler 中的等待也占住该线程。
- **C++ gRPC 的异步模型有哪些？** 传统 CompletionQueue 状态机和较新的 Callback/Reactor API。
- **CompletionQueue 的 tag 和 Next 循环怎么工作？** 发起异步操作时附带自定义 tag，完成事件进入 CQ；线程调用 Next 取出 tag 和 ok，再据此推进对应 RPC 状态机。
- **异步是否一定吞吐更高？** 不一定。CPU 密集业务、锁热点不变或并发不高时收益有限；只有阻塞/调度成本确实占主导且实现合理时才可能提高吞吐。
- **同步调用如何避免线程被慢请求占满？** deadline、ResourceQuota、有界回源池、breaker 和限流共同保护；只设置客户端超时不足以取消服务端第三方调用。
- **deadline、cancellation、status 如何使用？** deadline 限制总等待时间；cancellation 是协作式取消信号；status 表示传输/框架结果，业务状态应独立表达。
- **常见状态如何映射？** UNAVAILABLE 对幂等读可退避重试并计节点 breaker；DEADLINE_EXCEEDED 通常直接降级并记录超时；RESOURCE_EXHAUSTED 表示背压，应限流或稍后重试，不能立即风暴式重放。

回答边界：
- 当前项目如果是同步 unary RPC，可以说明它实现简单、逻辑直观；并通过客户端连接复用、服务端工作线程限制和 deadline 控制资源。
- 异步 RPC 的价值是少量 I/O 线程管理大量 in-flight 请求，但状态机、生命周期和错误处理更复杂。不要声称项目使用了 CompletionQueue，除非确实实现过。

### 17.3 Channel、Stub 与连接复用

**面试官：Channel 和 Stub 分别是什么？为什么连接池复用 Channel？**

**回答：**Channel 是面向目标服务的传输和连接状态抽象，Stub 是由 proto 生成的类型安全 RPC 代理。当前按节点复用 shared_ptr<Channel>，每次 RPC 创建局部 Stub。设计目的在于复用连接、HTTP/2 状态和重连机制，而不是假设 Channel 就是一条永不变化的 TCP socket。

连环追问与回答：

- **Channel 是否等于一个 TCP 连接？** 不等于。它可管理 subchannel、地址解析和连接重建；在当前单地址简单场景通常落到一条活跃 HTTP/2 transport。
- **一个 Channel 是否可以同时承载多个 RPC？** 可以，HTTP/2 用多个 stream 多路复用。
- **Stub 是线程安全的吗？** 应以当前 gRPC 版本对具体 Stub API 的文档为准。项目使用每请求局部 Stub，不依赖跨线程共享 Stub。
- **连接新建会经历哪些成本？** 名称/地址解析、TCP 建连、TLS 握手（若启用）、HTTP/2 preface/settings 和 gRPC transport 初始化。
- **节点下线后，旧 Channel 如何清理或失效？** watch DELETE 从 map erase；in-flight RPC 的 shared_ptr 保活到完成。未显式取消的请求继续发出或等待 deadline。
- **Channel pool 如何避免无限增长？** 当前依赖 DELETE 清理。生产还要设置最大节点数、idle TTL/LRU 回收，并处理 watch 丢失后的全量校准。

建议回答：
- Channel 是客户端到目标服务的一层抽象，底层可管理连接；不要机械地把它等同于“恰好一个 TCP socket”。
- HTTP/2 允许一个底层连接承载多个 stream，因此复用 Channel 能减少频繁建连和握手开销。

### 17.4 HTTP/2 协议边界

**面试官：HTTP/2 的多路复用和流控如何影响你的 gRPC 压测？**

**回答：**HTTP/2 允许少量 TCP 连接承载大量并发 RPC，但 stream 上限、连接/流窗口和服务端执行资源共同构成排队系统。压测中的 8 connections 和 128 concurrency 表示约把 128 个在途 RPC 分配到 8 条可复用连接，而不是每个请求创建连接。

连环追问与回答：

- **stream 是什么？一个 TCP 连接可否有多个 stream？** stream 是独立逻辑双向字节流，一条 HTTP/2 TCP 连接可以并发多个 stream。
- **SETTINGS_MAX_CONCURRENT_STREAMS 过小会怎样？** 超出部分等待可用 stream，连接利用率和 QPS 下降，P99 增加。
- **连接级和流级 flow control 分别限制什么？** 连接窗口限制所有 stream 的未确认数据总量；流窗口限制单个 stream，防止一个大消息垄断连接。
- **是否消除了 TCP 层队头阻塞？** 没有。HTTP/2 消除 HTTP/1.1 应用层串行，但所有 stream 共享 TCP 有序字节流，丢包会阻塞后续数据交付。
- **线程、stream、并发如何共同影响 P99？** 客户端并发产生需求，stream 决定 transport 接纳量，服务线程决定处理率；处理率低于到达率时请求在某层排队，尾延迟增长。
- **HPACK 解决什么问题？** 用静态/动态表压缩重复 HTTP header，降低每个 RPC 的元数据体积，但维护动态表也有少量 CPU 和状态成本。

掌握标准：
- 正确表述为 HTTP/2 在单连接上多路复用 stream，减少 HTTP/1.1 同连接串行请求造成的应用层阻塞。
- 底层仍是 TCP，发生丢包时仍可能有 TCP 层队头阻塞；不要说 HTTP/2 “完全没有队头阻塞”。

### 17.5 etcd：注册、租约和 Watch

**面试官：一个缓存节点从启动到下线，如何通过 etcd 被客户端感知？**

**回答：**节点启动时创建 10 秒 lease，把 /services/<service>/<addr> 写入 etcd 并绑定 lease，随后周期 keepalive。客户端先获取服务前缀快照，再 watch PUT/DELETE；节点崩溃后 lease 无法续约，etcd 到期删除 key，客户端收到 DELETE 后更新节点集、哈希环、breaker 和 Channel。

连环追问与回答：

- **key/value 如何设计，value 至少包含什么？** 当前 key 分层编码服务和地址，value 只存地址；生产至少还可包含权重、zone、版本、协议和健康元数据。
- **lease 和 keepalive 分别是什么？** lease 定义临时 key 生命周期，keepalive 延长该生命周期；心跳失败不是立即 DELETE，要等租约到期。
- **崩溃后为什么租约到期能删除注册？** lease 生命周期由 etcd 集群维护，不依赖节点执行退出清理，所以进程崩溃也能自动回收。
- **如何先全量拉取再持续 watch？** 正确模式是 Get Prefix 得到 snapshot 和 revision，原子替换本地视图，再从 revision+1 watch。
- **watch 从哪个 revision 开始？** 应从全量快照对应 revision 的下一版本开始，避免 list 与 watch 之间漏事件；当前封装未完整实现该保证。
- **compact 或网络异常后如何重同步？** 停止使用断裂 watch 的增量假设，重新拉全量快照并校准本地集合，再从新 revision 继续。
- **为什么旧节点列表可以短期兜底？** 数据面直接访问缓存节点，不需要每次查询 etcd；但列表越旧，命中下线节点和漏掉新节点的概率越高。
- **为什么不用 Redis 普通 key 做服务发现？** 可以自行实现，但 etcd 原生提供线性一致 KV、lease、revision 和可靠 watch 语义；Redis 需要额外设计心跳、过期和订阅断线补偿。

回答边界：
- etcd 的核心优势在于一致的 KV 视图、lease 和 watch，适合服务注册与配置分发。
- etcd 基于 Raft 获得强一致语义，但不代表客户端 watch 永远不丢事件；生产级实现需处理 revision、重连与 compaction 后的重同步。

### 17.6 Prometheus 指标与指标暴露

**面试官：Prometheus 如何采集你的缓存服务指标？**

**回答：**缓存节点在配置 metrics_port 后，用独立 HTTP Server 暴露 /metrics；Prometheus 按 scrape interval 主动拉取文本并存储时序，PromQL 再计算速率、聚合和告警。项目是手工生成 exposition format，不是把指标推送给 Prometheus。

连环追问与回答：

- **pull 模型的基本流程是什么？** 服务暴露 endpoint，Prometheus 定期 GET，解析样本并附加 target label，规则和 Grafana 再消费。
- **/metrics 暴露什么格式？** Prometheus text exposition 0.0.4，包含 HELP、TYPE 和带 label 的样本行。
- **四类指标分别用于什么？** Counter 用累计事件，Gauge 用瞬时状态，Histogram 用可聚合分布，Summary 用客户端分位数。
- **命中数、inflight、连接数、延迟分别用什么？** 命中数 Counter；inflight 和连接数 Gauge；RPC 延迟 Histogram。
- **P99 如何近似？** 对 bucket 的 rate 按 le 聚合后使用 histogram_quantile；结果落在 bucket 内插值，因此 bucket 设计决定精度。
- **label 为什么不能无限增加？** 每个 key/user 组合创建独立时序，基数随业务数据增长，会压垮 TSDB 和查询。
- **如何辅助排查击穿？** hit ratio 下降、getter invocation 和 waiter 同时上升说明 miss 被合并；若 breaker/fallback 随之上升，说明后端已受压。当前缺少部分 SingleFlight 指标，需要补齐才能完整判断。

建议掌握：
- Prometheus 主要是主动拉取指标，服务端暴露 `/metrics`；告警、查询、可视化通常由 PromQL 和外部组件完成。
- 要关注 label 基数，不能把无限变化的 key、请求 ID 作为 label。

### 17.7 CRC32 与哈希实现

**面试官：为什么用 CRC32 做一致性哈希？它有什么边界？**

**回答：**CRC32 计算快、跨语言容易保持一致，且当前兼容 Go crc32.ChecksumIEEE，适合可信内部 key 的路由实验。一致性哈希首先需要确定性、分布足够均匀和低成本；如果 key 来自不可信用户，还要考虑哈希洪泛和恶意碰撞，此时 CRC32 不够。

连环追问与回答：

- **CRC32 是加密哈希吗？** 不是，不能用于密码、签名或对抗碰撞攻击。
- **更关心均匀性、稳定性还是安全性？** 内部缓存路由优先确定性、均匀和性能；面对不可信输入时安全性也成为必要约束。
- **CRC32 碰撞如何处理？** 当前实现会覆盖同 hash 的映射，是局限；应使用碰撞桶、复合 key 或 64/128 位高质量 hash。
- **虚拟节点如何缓解倾斜？** 让每台机器持有多个分散小区间，降低单个随机区间大小的方差。
- **哈希环查找通常如何实现？** 有序数组加 lower_bound，或平衡树 lower_bound；到尾部回环到 begin。

回答边界：
- CRC32 计算快、实现简单，可用于非安全场景的散列定位；它不能用于安全校验或抵抗恶意碰撞。
- 当前项目用虚拟节点改善分布；如面对不可信 key 或更高分布质量要求，可评估其他哈希函数。

### 17.8 C++ 并发原语在第三方库调用中的使用

**面试官：gRPC、etcd watch、SingleFlight 同时存在时，哪些共享状态需要保护？**

**回答：**要先按所有权划分共享状态：节点集和客户端路由由 nodes_mutex_ 协调；哈希环内部由 shared_mutex 保护；Channel map 和 breaker pool 各有 mutex；SingleFlight map 有 mutex、Call 结果由 promise/shared_future 协调；CircuitBreaker 状态机由内部 mutex 保护。第三方库对象本身线程安全，不代表这些业务容器可以无锁访问。

连环追问与回答：

- **节点列表和哈希环更新如何保证一致？** 当前在 nodes_mutex_ 临界区同时变更 cache_nodes_ 和一致性哈希，环内部再用读写锁；更好的方案是构建不可变路由快照后原子替换。
- **inflight map 如何避免重复回源？** 在同一 mutex 下检查并创建 Call，先发布 Call 再释放锁执行 getter，消除两个线程同时判断不存在的竞态。
- **条件变量为什么搭配 while 谓词？** 因为虚假唤醒和竞争唤醒都可能使条件在拿锁时再次不成立；项目 SingleFlight 主要使用 future，但阻塞队列仍应遵守该规则。
- **回调线程能否长时间持锁或阻塞 RPC？** 不应。当前 Set 广播持 nodes_mutex_ 做串行 RPC 是已知问题，应先复制节点快照再释放锁。
- **熔断器计数适合原子还是互斥锁？** 单个独立 Counter 可用原子，但 breaker 的 state、generation、半开配额和多个计数必须作为整体满足不变量，因此当前 mutex 更清晰。
- **如何避免锁顺序死锁？** 减少嵌套锁、规定 nodes/channel/breaker 的固定顺序、复制 shared_ptr 或快照后释放外层锁，再调用 gRPC 或第三方回调；外部代码绝不能在持业务锁时调用。

建议：
- 不必把 gRPC 或 etcd 客户端的内部线程模型讲到源码级，但必须区分“库自身线程安全”和“你维护的节点表、哈希环、连接池、熔断器是否线程安全”。

### 17.9 学习优先级与自测标准

第一优先级：Protobuf 字段演进、gRPC 调用流程与 deadline、etcd lease/watch/revision、HTTP/2 stream/并发限制。

第二优先级：Channel 复用、Prometheus 指标类型和 label 基数、CRC32 与虚拟节点、SingleFlight 并发流程。

第三优先级：CompletionQueue 状态机、流式 RPC、Protobuf 详细编码规则。这些可了解，但在没有实际落地时不应成为简历主张。

自测方法：针对每一项，用 90 秒说明“它在项目中解决的问题、关键调用链、故障时的表现、当前实现的局限”。能自然讲清即达到校招面试所需深度。
